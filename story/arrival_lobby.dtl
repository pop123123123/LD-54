set {allow_moving} = 1
You enter the Lobby.

- Talk to the Warden [if {is_night} == 1][else="hide"]
	jump idle_warden/
- Talk to the Receptionist [if {is_night} == 0][else="hide"]
	jump idle_receptionist/
